// from data.js
var tableData = data;

console.log(tableData);

 //use userInput as search parameter for functs
 //iterate over array and do comparison
 //return new result array to 

function findDate(tableData, userInput){
   

    tableData.filter(tableData.datetime);
}

function findCity(tableData){
    tableData.filter(tableData.city);
}

function findState(tableData){
    tableData.filter(tableData.state);
}

function findCountry(tableData){
    tableData.filter(tableData.country);
}

function findShape(tableData){
    tableData.filter(tableData.shape);
}
